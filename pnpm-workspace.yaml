# Packages that trigger false-positive dependency warnings during install.
# core-js and esbuild are pulled in by transitive deps, not directly referenced.
ignoredBuiltDependencies:
  - core-js
  - esbuild
